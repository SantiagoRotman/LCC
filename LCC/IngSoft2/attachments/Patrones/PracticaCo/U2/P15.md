# Sistema de Archivos Linux

## Enunciado

Linux divide el sistema de archivos en dos capas. La capa superior, en contacto con las aplicaciones, provee una interfaz uniforme para acceder archivos; la inferior, en contacto con el hardware, consiste de uno o más sistemas físicos de archivos (SFA). La capa superior se conoce como sistema de archivos virtual (VFS).

El módulo correspondiente a cada SFA conoce los detalles del hardware de almacenamiento y se registra ante el VFS. Al hacerlo, el VFS puede redirigir adecuadamente las operaciones sobre archivos de ese SFA. Cada SFA agrupa y representa los archivos de una forma diferente, pero todos están obligados a presentar una interfaz compatible con aquella a nivel de usuario.

Considere el problema relativo al sistema de archivos de Linux visto en la práctica de diseño. El VFS da una representación jerárquica a los archivos y directorios como usted sabe. Utilice el patrón Composite para representar los directorios y archivos.

Aplique el patrón Iterator para recorrer el sistema de archivos de diferentes formas.

Suponga que el VFS provee un módulo que dado un código de error emitido por alguna de sus subrutinas (llamadas al sistema) retorna el mensaje correspondiente. Aplique el patrón Bridge para para poder configurar el VFS para retornar los errores en diferentes idiomas (internacionalización).

Una utilidad provista por Linux es du que retorna el espacio en disco utilizado por un directorio dado. Estas utilidades se proveen a nivel de usuario. Analice la posibilidad de aplicar (y eventualmente aplique) el patrón Visitor para implementar este tipo de servicios (find, etc.).

Otra utilidad provista por Linux es el comando fscheck que permite reparar el sistema de archivos ante algunos daños. Analice la posibilidad de aplicar (y eventualmente aplique) el patrón Strategy para implementar diferentes estrategias de reparación. Haga lo mismo con el patrón Visitor. Notar que se requiere recorrer el sistema de archivos completo

## Módulos 2MIL

```
MODULE  SFA
EXPORTS write(i Direction, i *Buffer, i Len)
        read(i Direction, i *Buffer, i Len)
```

```
MODULE SFA1 INHERITS FROM SFA
```

```
MODULE SFA2 INHERITS FROM SFA
```

```
MODULE  Iterator
EXPORTS first()
        next()
        isDone?() : Bool
        current() : Component
```

```
MODULE  DFS INHERITS FROM Iterator
```

```
MODULE  BFS INHERITS FROM Iterator
```

```
MODULE  Component
EXPORTS delete()
        rename(i String)
        addChildren(i Component)
        createIterator() : Iterator
        accept(i Visitor)
```

```
MODULE  Directory INHERITS FROM Component
```

```
MODULE  File INHERITS FROM Component
EXPORTS read() : Content
        update(i Content)
        getSize() : Int
```

```
MODULE  Visitor
EXPORTS visitFile(i File)
        visitDirectory(i Directory)
```

```
MODULE  Du INHERITS FROM Visitor
```

```
MODULE  Find INHERITS FROM Visitor
```

```
MODULE  VFS
IMPORTS SFA, Component, ErrorLog
EXPORTS touch(i Path, i Content)
        mkdir(i Path, i Name)
        read(i Path) : Content
        update(i Path, i Content)
        ls(i Path)
        rm(i Path)
        mv(i Path, i Path)
        rename(i Path, i String)
        register(i SFA)
```

```
MODULE  ErrorLog
IMPORTS LanguageLog
EXPORTS defineLanguage(i LanguageLog)
        log(i Error)
```

```
MODULE  LanguageErrorLog
EXPORTS translateErrorType(i ErrorType) : String
        translateVariable(i Variable) : String
```

```
MODULE  EnglishErrorLog INHERITS FROM LanguageErrorLog
```

```
MODULE  SpanishErrorLog INHERITS FROM LanguageErrorLog
```