# Teléfonos celulares

## Enunciado 

Una compañía desarrolla software para teléfonos celulares. Uno de los problemas que más costo de mantenimiento tiene es el de la interacción con los dispositivos de cada teléfono. Todos los teléfonos tienen más o menos los mismos dispositivos que hacen más o menos las mismas cosas pero cada fabricante (y posiblemente cada modelo) lo hace de forma ligeramente diferentes.

Utilice el patrón de diseño Bridge para resolver este problema en lo referente a la pantalla y la placa de sonido. Debe quedar muy claro en su diseño la diferencia entre interfaz e implementación; en particular debe aclararse las
razones por las cuales cierta funcionalidad se ubica en la «interfaz» y otra en la «implementación».

## Módulos 2MIL

```
MODULE  InterfazPantalla
IMPORTS ImplementaciónPantalla
EXPORTS definirImplementación(i ImplementaciónPantalla)
        dibujar()
        ...
```

```
MODULE  ImplementaciónPantalla
EXPORTS cambiarPixel(i Pos, i RGB)
        ...
```

```
MODULE  ImplementaciónPantallaSamsung INHERITS FROM ImplementaciónPantalla
        ...
```

```
MODULE  ImplementaciónPantallaXiomi INHERITS FROM ImplementaciónPantalla
        ...
```

```
MODULE  InterfazPlacaDeSonido
IMPORTS ImplementaciónPlacaDeSonido
EXPORTS definirImplementación(i ImplementaciónPlacaDeSonido)
        emitir()
        ...
```

```
MODULE  ImplementaciónPlacaDeSonido
EXPORTS emitirBanda(i Banda)
        ...
```

```
MODULE  ImplementaciónPlacaDeSonidoSamsung INHERITS FROM ImplementaciónPlacaDeSonido
        ...
```

```
MODULE  ImplementaciónPlacaDeSonidoXiomi INHERITS FROM ImplementaciónPlacaDeSonido
        ...
```


## Documentación de patrones

Pattern: Componentes Pantalla
Based on: Bridge
Because:
Where: 
    - InterfazPantalla is Abstraction
    - ImplementaciónPantalla is Implementor
    - ImplementaciónPantallaSamsung is ConcreteImplementorA
    - ImplementaciónPantallaXiomi is ConcreteImplementorB


Pattern: Componentes PlaceDeVideo
Based on: Bridge
Because:
Where: 
    - InterfazPlaceDeVideo is Abstraction
    - ImplementaciónPlaceDeVideo is Implementor
    - ImplementaciónPlaceDeVideoSamsung is ConcreteImplementorA
    - ImplementaciónPlaceDeVideoXiomi is ConcreteImplementorB