# Videojuego

## Enunciado

Un juego de computadora medianamente complejo consiste en que un personaje tome ciertos objetos evitando ser capturado por diversos tipos de enemigos o caer en trampas. El escenario donde el personaje actúa consiste
de habitaciones con puertas, escaleras, ascensores, tubos para descender, etc.

En cada escenario hay un cierto número de objetos que el personaje debe tomar. Cuando el personaje los toma a todos, el juego pasa de nivel a otro escenario semejante al anterior pero diferente (más complejo). Esto se repite al inﬁnito.

El jugador puede mover el personaje en todas las direcciones, puede ordenarle que suba, baje, tome un ascensor, tome un objeto, etc. A medida que el jugador pasa de nivel, el personaje puede contar con ciertas armas para defenderse de los enemigos.

Los enemigos aparecen y desaparecen de forma dinámica dependiendo de lo que haga el personaje. Las trampas son ﬁjas pero pueden activarse o desactivarse según lo que haga el personaje; el jugador tiene que ver en la pantalla si una trampa está activada o no.

1. Aplique el patrón de diseño Composite para representar la estructura física de los niveles y escenarios del videojuego.

2. Aplique el patrón de diseño Decorator para dotar de armas al personaje del videojuego.

## Módulos 2MIL

```
MODULO  Componente
EXPORTS agregarComponenteHijo(i Componente)
        eliminarComponenteHijo(i Componente)
        obtenerComponenteHijo(i Int)
        generar()
```

```
MODULO  Nivel INHERITS FROM Componente
```

```
MODULO  Escenario INHERITS FROM Componente
```

```
MODULO  Habitación INHERITS FROM Componente
```

```
MODULO  Escenografía INHERITS FROM Componente
```

```
MODULO  Objeto INHERITS FROM Componente
```

```
MODULO  Enemigo INHERITS FROM Componente
```

```
MODULO  Trampa INHERITS FROM Componente
```

```
MODULE  Jugador
EXPORTS atacar()
```

```
MODULE  Guerreo INHERITS FROM Jugador
```

```
MODULE  Caballero INHERITS FROM Jugador
```

```
MODULE  Arma INHERITS FROM Jugador
EXPORTS guardarJugador(i Jugador)
```

```
MODULE  Hacha INHERITS FROM Arma
```

```
MODULE  Espada INHERITS FROM Arma
```

## Documentación

Pattern: Niveles
Based on: Composite
Because:
Where: 
    - Componente is Component
    - Nivel is Composite
    - Escenario is Composite
    - Habitación is Composite
    - Escenografía is Leaf
    - Objeto is Leaf
    - Enemigo is Leaf
    - Trampa is Leaf
    - generar() is operation()

Pattern: Jugador
Based On: Decorator
Because:
Where:
    - Jugador is Component
    - Guerreo is ConcreteComponent
    - Caballero is ConcreteComponent
    - Arma is Decorator
    - Hacha is ConcreteDecorator
    - Espada is ConcreteDecorator
    - atacar() is operation()