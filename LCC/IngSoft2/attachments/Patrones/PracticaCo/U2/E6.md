# Cuadros de diálogo

## Enunciado 

Un cuadro de diálogo toma dos datos que ingresa el usuario. Cuando el usuario pulsa el botón «Aceptar» se debe consultar un archivo y con los datos que allí se encuentren se debe abrir otro cuadro de dialogo que muestre
algunos de esos datos.

Suponga que el código que muestra el primer cuadro de diálogo y lee la entrada del usuario está agrupado en el módulo CuadroD1; la función que consulta el archivo está en el módulo Consult; y el código del segundo cuadro
de diálogo en el módulo CuadroD2.

Utilice el patrón de diseño Command para implementar la interacción entre CuadroD1 y CuadroD2 y Consult. Muestre el pseudo-código que implementaría su diseño. Muestre con detalle cómo sería el flujo de datos entre los tres módulos y los que usted defina.

## Módulos 2MIL

```
MODULE  BotónAceptar
IMPORTS Comando
EXPORTS inicializar()
```

```
MODULE  Comando
EXPORTS ejecutar()
```

```
MODULE  LecturaArchivo INHERITS FROM Comando
IMPORTS CuadroD1, CuadroD2, Consult
```

```
MODULE  CuadroD1
EXPORTS mostrar()
        leer() : Datos
        ...
```

```
MODULE  Consult
EXPORTS consultar(i archivo) : Datos
        ...
```

```
MODULE  CuadroD2
EXPORTS generar(i Datos)
        ...
```

```
MODULE  Sistema
IMPORTS Comando, CuadroD1, CuadroD2, Consult, BotónAceptar
EXPORTS inicializar()
```

## Pseudo-código

LecturaArchivo

def ejecutar() {

    archivo = CuadroD1.leer()
    datos = Consult.consultar(archivo)
    CuadroD2.generar(datos)
}

## Documentación de patrones

Pattern: Lectura de Archivo
Based On: Command
Because:
Where:
    - Comando is Command
    - LecturaArchivo is ConcreteCommand
    - BotónAceptar is Invoker
    - CuadroD1 is Receiver
    - Consult is Receiver
    - CuadroD2 is Receiver
    - Sistema is Client